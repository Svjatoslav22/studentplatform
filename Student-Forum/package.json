{
  "name": "student-platform",
  "version": "1.0.0",
  "description": "Student Platform - Timetable, Books, Past Papers, Q&A",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start-server\" \"npm run start-client\"",
    "start-server": "cd server && node server.js",
    "start-client": "cd client-side && cross-env NODE_OPTIONS=--openssl-legacy-provider npm start",
    "seed": "cd server && node seed_timetable.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
